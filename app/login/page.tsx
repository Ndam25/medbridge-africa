export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function LoginPage() {
  return (
    <main style={{padding:'4rem'}}>
      <h1>Connexion</h1>
      <p>Page de connexion (version minimale). Formulaire à venir.</p>
    </main>
  );
}
